# Empty = same origin (use Vite dev proxy to the API). Set full URL if calling the API directly.
VITE_API_BASE_URL=


# Optional: override API origin for Vite dev proxy (see vite.config.ts).
VITE_API_BASE_URL_WS=ws://localhost:8000